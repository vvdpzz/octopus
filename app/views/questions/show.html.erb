<% j = JSON.parse(@result) %>
<%= j['question']['title'] %>
<hr>
<%= j['answers'][0]['content'] %>
<%= j['answers'][1]['content'] %>
<%= j['answers'].length %>
<%= form_for(Comment.new, :url => "/questions/#{params[:id]}/comments") do |f| %>
	<%= f.text_area :content %>
	<%= f.submit 'Create an Comment' %>
<% end %>

<%= form_for(Answer.new, :url => "/questions/#{params[:id]}/answers") do |f| %>
	<%= f.text_area :content %>
	<%= f.submit 'Create an Answer' %>
<% end %>